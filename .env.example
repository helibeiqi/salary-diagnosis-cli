# 本文件为密钥与环境变量样例。复制为 .env 后填入真实值；
# .env 已被 .gitignore 拦截，绝不进仓库。
#
# 所有变量均为「可选」：不填则使用 config.yaml 的本地默认
# （ollama-local，且 models.fallback.enabled=false，不会自动上云）。

# —— 可选云端模型 ——
# 仅在 env 显式配置了密钥「且」config.yaml 中 models.fallback.enabled=true 时才可能启用；
# 默认值下即使填了密钥也走本地模型（fail-safe）。
# OPENAI_API_KEY=
# OPENAI_BASE_URL=

# —— 遥测（可选，默认关；设为 1 完全关闭本地遥测写盘）——
# COMP_TELEMETRY_OFF=1
